# Generated by Cargo
# will have compiled files and executables
debug/
target/

# Remove Cargo.lock from gitignore if creating an executable, leave it for libraries
# More information here https://doc.rust-lang.org/cargo/guide/cargo-toml-vs-cargo-lock.html
Cargo.lock

# These are backup files generated by rustfmt
**/*.rs.bk

# MSVC Windows builds of rustc generate these, which store debugging information
*.pdb

# SQLite files.
*.db

# It doesn't have any config variables, but it has DATABASE_URL set. That might mess up the compilation if you don't already
# have a database with the tables setup correctly.
.env

# auto-generated buffers by emacs
*.tmp

# All the files on the node.
media/

# Config file, to prevent overwrites on update.
comet-config.toml
